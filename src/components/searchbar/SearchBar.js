import React from 'react';

const SearchBar = () => {
  return (
    <input
      type="text"
      className="w-[100%] rounded-full h-[38px] px-2 outline-none"
      placeholder="Search"
    />
  );
};

export default SearchBar;
